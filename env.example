# Example environment variables for Race-OS (financial-hub)
# Copy this file to .env and fill secrets before running locally or in CI
# Sensitive values SHOULD NOT be committed to git. Use a proper secret store for production.

######### Backend #########
# Server port the backend listens on
PORT=5000

# Node environment: development | production | test
NODE_ENV=development

# URL of the frontend app allowed by CORS (backend uses this)
# Default used in code when missing: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# MongoDB connection string. If not set the project may use an in-memory DB for local tests.
# Example (local): mongodb://localhost:27017/financial-hub
MONGODB_URI=

# JWT secret used to sign authentication tokens (KEEP SECRET)
JWT_SECRET=change_this_to_a_random_long_secret

# JWT token lifetime (e.g. 7d, 1h)
JWT_EXPIRE=7d

######### PDF Generation #########
# Set to 'true' to disable PDF generation in development (useful for Codespaces/containers)
DISABLE_PDF_GENERATION=false

# Custom path to Chrome/Chromium executable (if needed)
PUPPETEER_EXECUTABLE_PATH=

######### Email Configuration (for invoice sending) #########
# Email service credentials for sending invoices
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# SendGrid API key (alternative to EMAIL_USER/PASS)
SENDGRID_API_KEY=

######### Plaid (optional integration) #########
# Environment: sandbox | development | production
PLAID_ENV=sandbox

# Plaid API credentials (if using Plaid integration)
PLAID_CLIENT_ID=
PLAID_SECRET=

######### Notes #########
# - Copy this file to .env and update values for local development.
# - Do NOT commit the real .env to the repository. Add .env to .gitignore if you want.
# - For CI/CD, inject secrets through your pipeline / deployment platform.
# - For Gmail: use App Passwords instead of your regular password
# - For production: consider using SendGrid, AWS SES, or similar email service
# - If PDF generation fails in containers, set DISABLE_PDF_GENERATION=true for development
